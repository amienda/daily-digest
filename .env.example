# ---- Frontend (bundled into the browser; safe-by-design public keys only) ----
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxx

# ---- Server-side only (NEVER prefix with VITE_; these stay on the server) ----
# Used by /api/instapaper-save.ts inside the Vercel serverless function.
INSTAPAPER_USERNAME=your-instapaper-email@example.com
INSTAPAPER_PASSWORD=your-instapaper-password
